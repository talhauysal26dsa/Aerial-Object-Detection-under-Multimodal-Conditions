# Aerial Object Detection under Multimodal Conditions

Project page source for GitHub Pages is under `docs/`.

## Publish
- Push to `master` branch.
- In repository settings, set GitHub Pages source to `Deploy from branch` -> `master` -> `/docs`.

